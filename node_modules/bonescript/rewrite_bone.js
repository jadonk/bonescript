require('bonescript');

for(pin in bone) {
    var mode = getPinMode(bone[pin]);
    //console.log('getPinMode(' + bone[pin].key + ') = ' + JSON.stringify(mode));
}

console.log("if(typeof exports === 'undefined') exports = {};");
console.log("");
console.log("exports.bone = " + JSON.stringify(bone,null,4) + ";");
